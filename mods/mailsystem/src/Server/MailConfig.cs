namespace mailsystem.src.Server
{
    public class MailConfig
    {
        public int MaximumMailsPerPlayer;
        public MailConfig(int maxMailsPerPlayer)
        {
            MaximumMailsPerPlayer = maxMailsPerPlayer;
        }
    }
}
